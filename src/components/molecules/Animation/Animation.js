import React, { useRef } from 'react';
import 'styles'

const Animation = () => {
    const myRef = useRef(null);

    return (
        <div class="element">

        </div>
    );
};

export default Animation;
