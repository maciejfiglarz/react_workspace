import styled from 'styled-components';
import Button from '../../Components/Button';

const MyButton = styled.button`
  background: red;
`;
const Root = () => <MyButton>test<Button>ddd</Button></MyButton>;

export default Root;
